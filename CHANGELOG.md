# CHANGELOG

В этом файле фиксируются релизы, которые были выложены в production.

## Формат

Каждый релиз оформляется отдельным блоком:

```md
## YYYY-MM-DD

- Версия или commit: <commit_sha>
- Что вошло в релиз
- Важные изменения в данных, миграциях или инфраструктуре
```

## Unreleased

- Упрощён текст раздела `Помощь`: из него убраны окружение и списки команд.
- Изменения в работе, которые еще не были выложены в production.

## 2026-05-19

- Версия или commit: `0.20.2`
- Исправлено падение production-бота при сохранении воды: в `MySQLStore` восстановлен недостающий helper для работы с cursor.
## 2026-05-19

- Версия или commit: `0.20.1`
- Для обычных пользователей скрыты служебные slash-команды из меню Telegram и из `/help`.
## 2026-05-19

- Версия или commit: `0.20`
- Удалён весь функционал, связанный с финансовыми данными, включая backend-обработку и storage API.
- Удалены Telegram-команды `/finance_month` и `/import_tbank`, а загрузка документов в чат отключена.
## 2026-05-18

- Версия или commit: `0.19`
- Обновлён Telegram-онбординг: сокращены тексты, переставлены кнопки, обновлены изображения шагов и поправлен экран после завершения онбординга.
- Из интерфейса убраны шаги, сон и связанные с Google Drive команды; эти данные больше не показываются в Telegram и Mini App, а background-импорт шагов отключён.
- Версия или commit: `0.18.5`
- В расчёте воды теперь учитываются только записи из `water_entries`; сохранённая вода в `meals` остаётся в данных, но больше не влияет на дневной прогресс и digest.
## 2026-05-18

- Версия или commit: `0.18.4`
- Ускорено сохранение воды в Telegram-боте: быстрый ответ `/water <мл>` больше не пересобирает полную дневную сводку и использует облегчённый snapshot прогресса по воде.
## 2026-05-18

- Версия или commit: `0.18.3`
- Существенно ускорено сохранение фото еды: для high-confidence сценария запись теперь сохраняется напрямую без промежуточного черновика, а ответ после фото формируется заметно быстрее.
- Оптимизированы внутренние операции после фото: облегчён summary для быстрого сценария и `decision_log` теперь пишет решения batched multi-row insert'ом.
- Для staging-окружения добавлена детальная отладка времени ответа, включая breakdown по этапам автосохранения.
## 2026-05-17

- Версия или commit: `0.18.2`
- На главном экране и экране загрузки Mini App обновлены тексты и брендинг: убран технический заголовок, `ai-me` заменен на `Что я ем!`, а кнопка открытия приложения сокращена до `Открыть`.
## 2026-05-17

- Версия или commit: `0.18.1`
- В Telegram-боте добавлен онбординг с иллюстрациями, который теперь открывается по `/start` и помогает быстро понять основной сценарий логирования еды.
- Упрощены тексты стартового экрана и раздела `Как это работает`, а подпись кнопки Mini App сокращена до `Открыть`.
## 2026-05-17

- Версия или commit: `0.18`
- В Telegram-боте добавлен этап 08 UX roadmap: после фото еды включены умные уточнения по уверенности распознавания и опциональные коучинговые комментарии.
- Для high-confidence сценария убран лишний шаг сохранения: прием пищи теперь может сохраняться сразу, а пользователь получает только одну финальную карточку записи.
- После автосохранения и ручного сохранения унифицировано редактирование сохранённой записи: появились `Порция`, ручной ввод коэффициента порции и единый сценарий `Изменить / Отмена`.

## 2026-05-17

- Версия или commit: `0.17`
- В Mini App реализован седьмой этап UX roadmap: появились разделы `Сегодня`, `История`, `Аналитика` и `Профиль`, а архив записей и распознаваний объединён в единый пользовательский сценарий.
- Добавлены аналитические payload’ы и профильные API для Mini App: дневные графики по калориям, белку и воде, streak/logging metrics, а также редактирование параметров, целей и напоминаний из приложения.
- Возвращена видимая анимация загрузки Mini App: экран загрузки теперь гарантированно показывается короткое время даже при очень быстром bootstrap.

## 2026-05-17

- Версия или commit: `0.16`
- В Telegram-боте реализован шестой этап UX roadmap: полноценный раздел `Профиль` с ветками `Обо мне`, `Цели` и `Напоминания`.
- Пользователь теперь может сохранить персональные параметры, настроить цели по воде, белку и калориям, а также управлять напоминаниями без Mini App.
- Новые настройки сохраняются в storage и начинают использоваться как дефолтные цели по воде и белку в ежедневных сводках, если отдельная цель на день не задана.

## 2026-05-17

- Версия или commit: `0.15`
- В Telegram-боте обновлен этап 04 UX roadmap: тяжелая история в чате заменена на быстрый recovery последней записи и понятный переход в Mini App.
- В Mini App реализован этап 05 UX roadmap: архив приемов пищи и распознаваний, разделы `Сегодня / История / Распознавания / Еще`, карточки записей и пустые состояния.
- Mini App открыт для обычных пользователей, а карточки истории больше не падают, если у web-service временно недоступен bucket.
- На экране `Сегодня` убран блок `Шаги за вчера`, а экран загрузки Mini App получил анимацию.

## 2026-05-12

- Версия или commit: `0.14`
- В Telegram-боте реализованы первые три этапа нового UX roadmap: новый welcome/home screen, action-first навигация, отдельная кнопка `Как это работает` и переработанная reply-клавиатура.
- Добавлен быстрый сценарий логирования воды: пресеты, ручной ввод объема, дружелюбная валидация и быстрые действия после сохранения.
- Добавлено редактирование food draft перед сохранением: можно менять название, порцию, время, состав и КБЖУ, а сценарий `Не то блюдо` теперь переписывает черновик по тексту пользователя.

## 2026-05-12

- Версия или commit: `0.13.1`
- Добавлен новый релизный поток через [scripts/release_main.py](/Users/kurinniy/Documents/Projects/ai-me/scripts/release_main.py): он поднимает версию на `main`, обновляет `CHANGELOG.md`, прогоняет `release_guard` и создаёт git tag релиза.
- Вынесены release helpers в [src/ai_me/release_tools.py](/Users/kurinniy/Documents/Projects/ai-me/src/ai_me/release_tools.py), а правила релизов в [AGENTS.md](/Users/kurinniy/Documents/Projects/ai-me/AGENTS.md) обновлены под tag-based workflow.

## 2026-05-12

- Версия или commit: `0.13`
- Daily digest получил новый пользовательский формат: жирный заголовок, отдельный блок КБЖУ, форматирование чисел и воды, а список блюд разбит по времени суток на утро, день, вечер и ночь.
- Для пустых блоков утро/день/вечер теперь выводится `не было записей`, ночной блок скрывается, если записей нет, а резюме отделено от сводки пустой строкой.
- В тексте daily commentary исправлены формулировки: добавлено корректное склонение слова `блюдо`, а сравнение теперь пишется как `база последних 7ми дней`.

## 2026-05-12

- Версия или commit: `0.12.2`
- Удалён legacy-путь хранения фото еды в MySQL: новые фото теперь сохраняются только в Railway Bucket, а в `meal_media` остаются только metadata и ссылки на bucket objects.
- MySQL schema cleanup теперь дропает колонку `meal_media.image_bytes`, а obsolete скрипт `scripts/migrate_meal_media_to_bucket.py` удалён из репозитория.
- Тесты и health-service обновлены под обязательный bucket-backed storage без fallback на `db_blob`.

## 2026-05-11

- Версия или commit: `0.12.1`
- Оптимизирован скрипт `scripts/migrate_meal_media_to_bucket.py`: обновление `meal_media` в MySQL теперь выполняется batched `executemany(...)` с одним `commit` на батч вместо отдельного `UPDATE` на каждую запись.
- В лог миграции добавлены тайминги по этапам `upload`, `db_update` и `total`, чтобы быстрее диагностировать реальный bottleneck при переносе фото.

## 2026-05-11

- Версия или commit: `0.12`
- Фото еды переведены на dual-format storage: новые записи могут сохраняться в Railway Bucket, а legacy BLOB из MySQL продолжают читаться без отдельного cutover.
- В `meal_media` добавлены bucket metadata (`storage_key`, `bucket_name`, `width`, `height`), а digest и food flows научились догружать изображения из bucket только там, где это реально нужно.
- Добавлен отдельный скрипт `scripts/migrate_meal_media_to_bucket.py` для батчевого переноса старых `image_bytes` из MySQL в Railway Bucket.
- Конфигурация переведена на Railway-style bucket variables: `BUCKET`, `ENDPOINT`, `ACCESS_KEY_ID`, `SECRET_ACCESS_KEY`, `BUCKET_KEY_PREFIX`.

## 2026-05-11

- Версия или commit: `0.11`
- Weekly digest полностью переделан в фиксированную сетку `3x3`: дни недели раскладываются по постоянным ячейкам, а для дней без фото используется растровый placeholder; 7-я и 9-я ячейки оставлены пустыми.
- Daily и weekly digest получили расширенную и унифицированную отладку, включая детальные этапы сборки и параметр `prev` для `/weekly_digest_preview`.
- Оптимизированы daily и weekly digest: снижено число повторных чтений из БД, отдельным путём считаются вода и шаги, а для daily digest догружается только основное изображение каждого блюда.
- Проба с `MySQL connection pool` была отменена и не вошла в итоговую production-конфигурацию.

## 2026-05-10

- Версия или commit: `0.10`
- На daily- и weekly-мозаики фотографий в digest добавлены дата и период отчёта.
- Daily- и weekly-мозаики теперь визуально различаются: используют разные цвета рамки и плашки, а плашки располагаются в разных углах.
- Для администратора в daily и weekly digest добавлен debug-блок с временем рендера изображения и общим временем генерации ответа.
- Исправлен `/weekly_digest_preview` без аргументов: теперь он показывает текущую неделю.
- Для weekly digest исправлен рендер кириллицы на плашке периода за счёт явной установки системного шрифта в Docker-образ.
- Плашки digest ограничены по ширине и оставлены однострочными.

## 2026-05-09

- Версия или commit: `0.9`
- Для обычных пользователей добавлен rate limit на отправку фото еды: бот ограничивает слишком частые фото и не запускает параллельную обработку второго снимка, пока первый ещё анализируется.
- Google Drive import шагов стал обновляемым: если у уже импортированного файла изменился `checksum`, данные за этот файл переимпортируются и заменяют старую activity-запись в БД.
- Digest worker теперь по умолчанию опрашивает систему раз в 2 часа, проверяет только последние 2 дня файлов Google Drive, сохраняет время последнего успешного импорта и отправляет админам alert, если импорт не был успешным больше 24 часов.

## 2026-05-08

- Версия или commit: `0.8`
- Mini App закрыт для обычных пользователей и доступен только администраторам.
- Telegram-бот больше не рекламирует Mini App обычным пользователям, а кнопка `Открыть приложение` выставляется только администратору.
- Web backend Mini App возвращает `403 mini_app_admin_required` для обычных пользователей и для админов, которые временно переключились в user mode.

## 2026-05-08

- Версия или commit: `0.7`
- Администратор теперь может переключаться в режим обычного пользователя и обратно через `/user_mode` и `/admin_mode`.
- В режиме обычного пользователя у администратора скрываются admin-only команды и кнопки, а daily digest отправляется без блока шагов.
- Обновлена синхронизация Telegram menu commands: бот публикует команды и для default, и для `ru`, чтобы новые команды корректно появлялись в русскоязычном клиенте.

## 2026-05-08

- Версия или commit: `0.6`
- Финансы и импорт шагов из Google Drive ограничены только для администраторов.
- Для обычных пользователей help, меню и reply-клавиатура очищены от админских разделов, а daily digest больше не показывает блок со шагами.

## 2026-05-08

- Версия или commit: `0.5`
- Убрана система инвайтов: бот и Telegram Mini App теперь автоматически создают пользователя при первом `/start` или первом входе.
- Help, onboarding и меню команд переведены на открытый режим доступа, при этом private-chat only и изоляция данных по `user_id` сохранены.

## 2026-05-07

- Версия или commit: `0.4`
- В счётчике воды теперь учитывается вода, распознанная в подтверждённых фото-приёмах пищи.
- Food analysis, черновики еды и MySQL-схема расширены полем `water_ml`, чтобы вода из еды попадала в дневную сводку без ручного логирования.

## 2026-05-07

- Версия или commit: `0.3`
- Продуктовая версия обновлена до `0.3` после релиза Telegram Mini App и переработки `/summary`.
- Добавлен обязательный release guard: production-релиз теперь должен обновлять `src/ai_me/version.py`, `frontend/package.json` и верхний релизный блок `CHANGELOG.md`.

## 2026-05-07

- Версия или commit: `477d233`
- Добавлен foundation для Telegram Mini App: FastAPI backend, React/Vite frontend, аутентификация через Telegram Mini App `initData` и отдельный web runtime.
- Бот теперь синхронизирует кнопку `Открыть приложение`, а Mini App открывается через отдельный Railway web-service.
- Ускорен запуск Mini App: стартовый сценарий переведен на bootstrap flow, убран лишний запрос dashboard и упрощен payload первого экрана.
- Из Mini App убраны секции финансов, digest, Google Drive и черновиков; в интерфейсе оставлены summary, шаги за вчера, еда и открытые решения.
- Команда `/summary` упрощена: удалены блок еды, строки `Активность` и `Вес`.

## 2026-05-07

- Версия или commit: `0.2`
- Продуктовая версия обновлена до `0.2` после релиза Google Drive health import и расширения `/summary`.
- В `/help` теперь отображается актуальная версия `0.2`.

## 2026-05-07

- Версия или commit: `614a8c7`
- Добавлен импорт daily health data из Google Drive: подключение папки на пользователя, хранение настроек и история импортированных файлов.
- Digest worker теперь дополнительно сканирует подключенные папки Google Drive и автоматически импортирует новые JSON-файлы с activity data.
- В Telegram добавлены команды `/connect_drive`, `/drive_status`, `/drive_on`, `/drive_off`.
- Ошибки доступа к папке Google Drive переведены в понятные сообщения для пользователя.
- В `/summary` добавлены шаги за вчера, сравнение с 30-дневной средней и комментарий относительно целевого показателя.
- Дефолтная цель шагов изменена на `10000`.

## 2026-05-07

- Версия или commit: `f3371b3`
- В `/help` добавлены текущая версия приложения и дата релиза.
- Введен единый источник версии в `src/ai_me/version.py`.
- Текущая продуктовая версия зафиксирована как `0.1`.

## 2026-05-07

- Версия или commit: `8f596fa`
- Улучшена структура комментариев для daily и weekly digest.
- В digest добавлены структурированные commentary data: comparisons, meal pattern, macro balance, streaks, weekly patterns, consistency и highlight summary.
- Rule-based тексты daily и weekly digest теперь строятся на основе этих метрик и дают более содержательные аналитические выводы.

## 2026-05-07

- Версия или commit: `0e5a3b7`
- Добавлен framework для daily и weekly food digest с хранением фото еды и генерацией мозаики.
- Добавлены preview-команды digest в Telegram и отправка digest как пары сообщений: фото и текст.
- Добавлен отдельный digest worker c расписанием на `08:00`, идемпотентной отправкой через `digest_runs` и режимом запуска `APP_RUNTIME_MODE=digest_worker`.
- Обновлены конфигурация и документация Railway для запуска отдельного worker-service в staging и production.

## 2026-05-07

- Версия или commit: `e707b56`
- Удалены ручные Telegram-команды `/water`, `/meal`, `/weight`, `/sleep`, `/activity`, `/goals`.
- Обновлены help-текст и документация бота под photo-first и import-first сценарии.

## 2026-05-07

- Версия или commit: `be9ddfe`
- Добавлен multi-user режим с invite-only onboarding для Telegram-бота.
- Добавлены пользователи, инвайты, private-chat only режим и admin-команды для управления инвайтами.
- Данные health, food drafts, decisions и finance переведены на изоляцию по `user_id`.
- Добавлена автомиграция MySQL-схемы с привязкой legacy-данных к owner-аккаунту `96445950`.
